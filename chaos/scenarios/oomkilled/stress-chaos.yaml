apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: reviews-oom-stress
spec:
  mode: one
  selector:
    labelSelectors:
      app: reviews
  stressors:
    memory:
      workers: 1
      size: 1024MiB
      oomScoreAdj: -1000
  duration: "30s"
