apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: chaos-oom-stress
spec:
  mode: one
  selector:
    labelSelectors:
      app: adservice
  stressors:
    memory:
      workers: 1
      size: 1024MiB
      oomScoreAdj: -1000
  duration: "30s"
