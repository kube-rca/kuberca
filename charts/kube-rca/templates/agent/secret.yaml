apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kube-rca.agent.name" . }}-secret
  labels:
    {{- include "kube-rca.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
type: Opaque
stringData:
  # This is a mock secret for the agent
  api-key: "mock-api-key-12345"
