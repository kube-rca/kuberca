{{- $gemini := default (dict) .Values.agent.gemini -}}
{{- $geminiSecret := default (dict) $gemini.secret -}}
{{- $geminiExisting := default "" $geminiSecret.existingSecret -}}
{{- $geminiApiKey := default "" $gemini.apiKey -}}
{{- if and (eq $geminiExisting "") (ne $geminiApiKey "") -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kube-rca.agent.geminiSecretName" . }}
  labels:
    {{- include "kube-rca.agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ default "ai-studio-api-key" $geminiSecret.key | quote }}: {{ $geminiApiKey | quote }}
{{- end }}

{{- $openai := default (dict) .Values.agent.openai -}}
{{- $openaiSecret := default (dict) $openai.secret -}}
{{- $openaiExisting := default "" $openaiSecret.existingSecret -}}
{{- $openaiApiKey := default "" $openai.apiKey -}}
{{- if and (eq $openaiExisting "") (ne $openaiApiKey "") -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kube-rca.agent.openaiSecretName" . }}
  labels:
    {{- include "kube-rca.agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ default "openai-api-key" $openaiSecret.key | quote }}: {{ $openaiApiKey | quote }}
{{- end }}

{{- $anthropic := default (dict) .Values.agent.anthropic -}}
{{- $anthropicSecret := default (dict) $anthropic.secret -}}
{{- $anthropicExisting := default "" $anthropicSecret.existingSecret -}}
{{- $anthropicApiKey := default "" $anthropic.apiKey -}}
{{- if and (eq $anthropicExisting "") (ne $anthropicApiKey "") -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kube-rca.agent.anthropicSecretName" . }}
  labels:
    {{- include "kube-rca.agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ default "anthropic-api-key" $anthropicSecret.key | quote }}: {{ $anthropicApiKey | quote }}
{{- end }}
